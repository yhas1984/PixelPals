# Resting pet / cushion depth

The home motion arrival tolerance (.35 logical pixels) left a pet fractionally
behind its bed's ground plane. Strict depth sorting could then paint the cushion
over the sleeping body. Arrival now finishes at the exact bounded destination.
Depth sorting treats supports within .5 logical pixels as the same plane, drawing
the decoration first; genuinely closer decorations retain foreground occlusion.

Focused JVM tests pass for bed arrivals on all three rows and depth comparisons,
along with scheduled wake tests. Debug APK builds successfully. No artwork changed.
Physical visual confirmation remains pending.
