# Home visual follow-up — 2026-09-06

This checkpoint follows the audit corrections. It is not full-plan acceptance or production approval.

## Corrections from Android-rendered reviews

- The review now advances all 15 pets through greeting, approach and actual arrival at a rear-row bed. The previous catalogue checked only stationary pets without furniture.
- Home sprites measure visible alpha bounds during loading, normalize the animation's scale and keep floor contact independent of transparent padding or source resolution. No pixel scans happen while drawing. This corrects the floating Menta transition observed on the NE2213.
- Furniture renders before or after the pet according to depth. A learned favorite bowl or bed is no longer treated as a toy by autonomous play.
- Ginger's left-facing source animation is mirrored into the shared facing convention. Patito uses its flutter frames instead of swimming in the room; Diablillo uses full-body flight frames instead of the cropped catalogue portrait.
- Encounter illustrations match the existing seed, mushroom and fallen-star stories. The forest illustration uses an outdoor clearing.
- New homes start with no desktop object. The equipped object's dialog offers **Remove from desktop / Quitar del escritorio**. Removal updates the running overlay, keeps inventory/placement, and survives repository recreation. Previously equipped objects remain selected. No Room schema or SQL default changed.

## Evidence and limits

Initial decorated-room reviews were rendered on the NE2213 without accessing/resetting its database. Final images in `evidence/2026-09-06/home-transitions/` are NE2213 Canvas renders (including the final Ginger, Patito and Diablillo corrections); the encounter sheet is from the emulator. These prove the sampled poses, not uninterrupted physical animation quality. Some source art still differs in style between locomotion and care; full frame-by-frame artistic acceptance remains pending.

The care lab completed **30m 3.880s**, **388 scenes / 388 callbacks**, with unchanged PID. PSS ranged from **41,497 to 50,700 KiB**. The report is `evidence/2026-09-06/care-soak.json`. It ran the installed audit-fix build (8dfb34e), before this home-rendering follow-up. The care lab code/assets were not changed here. This is a care render/input soak, not a soak of the revised home, physical overlay, or battery usage.

The fixed-port phone endpoint temporarily stopped responding, then the same NE2213 reconnected through ADB mDNS. The final debug APK was installed with `install -r`, preserving user data. No destructive instrumentation ran on the phone. Play-track purchase/restore validation, physical lifecycle/accessibility review and production publication remain pending.

## Build checks

- Debug build and lint pass (no lint errors); 194 JVM tests pass.
- The full emulator suite passed 118 tests in 50.947 seconds before the final three source-animation choices. The affected scene suite then passed all 3 tests on the NE2213 in 5.674 seconds after the portable assertion adjustment.
- On API 36 the original byte-identical bitmap assertion failed because edge filtering differs by Android version. The regression now asserts visible geometry and the exact floor boundary, which is the rendering contract.
- Final candidate release APK/AAB build succeeded. `jarsigner -verify` validated the AAB signature; no upload/publication occurred. Artifact hashes are recorded in `evidence/2026-09-06/artifacts.json`.
